namespace BankingSystem.Test.Features.Users.CreateUser
{
    public class CreateUserTest
    {
        //_userManager.FindByEmailAsync(registerRequest.Email);

        //_userManager.CreateAsync(newUser, registerRequest.Password);

        //public void ShouldReturnUserIfitalreadyExists()
        //{
        //    var fakeUser = new UserEntity()
        //    {
        //        Email = "myEmail"
        //    };

        //    List<UserEntity> userList = new();

        //    A.CallTo(() => _userManager.CreateAsync(Ignored)).
        //        Invokes(() => userList.Add(fakeUser));

        //    _userManager.CreateAsync(fakeUser);

        //    A.CallTo(()=> _userManager.FindByEmailAsync(A<string>.Ignored))
        //        .ReturnsLazy((string email) =>
        //        {
        //            var found = userList.FirstOrDefault(u=>u.Email==email);
        //            return found;
        //        });

        //    var result = userservice.create();

        //    Assert.That(fakeUser, result);
        //}

    }
}
